server:
  port: 8080
spring:
  application:
    name: cepConsulta
  jpa:
    open-in-view: true
    show-sql: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/cepConsulta?allowPublicKeyRetrieval=true&sslMode=DISABLED
    username: root
    password: m1cr082